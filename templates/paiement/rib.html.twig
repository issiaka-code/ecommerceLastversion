{% extends 'base.html.twig' %}

{% block title %}Virement bancaire{% endblock %}

{% block body %}
<main>

    <!-- breadcrumb-area-start -->
    <div class="breadcrumb__area pt-5 pb-5">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="tp-breadcrumb__content">
                        <div class="tp-breadcrumb__list">
                            <span class="tp-breadcrumb__active"><a href="{{ path('app_home')}}">Home</a></span>
                            <span class="dvdr">/</span>
                            <span>Paiement</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- breadcrumb-area-end -->

    <!-- track-area-start -->
    <section class="track-area pb-40">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-6 col-sm-12">
                    <div class="table-responsive">
                        <table class="styled-table">
                            <thead class="" >
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Information de paiement</th>
                            </tr>
                            </thead>
                            <tbody>
                            <tr>
                                <th scope="row">TITULAIRE</th>
                                <td>{{rib[0].desti}}</td>
                            </tr>
                            <tr>
                                <th scope="row">IBAN </th>
                                <td>{{rib[0].adresse}}</td>
                            </tr>
                            <tr>
                                <th scope="row">BIC</th>
                                <td>{{rib[0].bic}}</td>
                            </tr>
                            <tr>
                                <th scope="row">BANQUE</th>
                                <td>{{rib[0].banque}}</td>
                            </tr>
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th >Montant a payer</th>
                                    <td>{{ montantTotal }} €</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>

                    <div class="mt-2" >
                        <h6>Continuer vers votre espace client  <a href="{{ path('app_compte')}}" class="text-warning"> cliquez ici </a></h6>
                    </div>
                </div>

            </div>
        </div>
    </section>
    <!-- track-area-end -->
    {% include 'plusInfo.html.twig' %}

</main>


{% endblock %}
