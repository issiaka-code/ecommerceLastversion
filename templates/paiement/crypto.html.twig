{% extends 'base.html.twig' %}

{% block title %}panier{% endblock %}

{% block body %}

    <main>

        <!-- breadcrumb-area-start -->
        <div class="breadcrumb__area pt-5 pb-5">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="tp-breadcrumb__content">
                            <div class="tp-breadcrumb__list">
                                <span class="tp-breadcrumb__active"><a href="{{ path('app_home')}}">Home</a></span>
                                <span class="dvdr">/</span>
                                <span>Paiement</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- breadcrumb-area-end -->

        <!-- track-area-start -->
        <section class="track-area pb-40">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-6 col-sm-12">

                        <div class="table-responsive">
                            <table class="styled-table">
                                <thead class="" >
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Information de paiement</th>
                                    <th scope="col">Action</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr>
                                    <th scope="row">{{crypto[0].type}}</th>
                                    <td> {{crypto[0].adresse}} </td>
                                    <td> <input type="button" class="btn btn-secondary" value="Copier" class="js-copy" data-target="#tocopy" style="margin-top:3px;  "> </td>
                                </tr>
                                <tr>
                                    <th scope="row">{{crypto[1].type}}</th>
                                    <td> {{crypto[1].adresse}} </td>
                                    <td> <input type="button" class="btn btn-secondary" value="Copier" class="js-copy" data-target="#tocopy" style="margin-top:3px;  "> </td>
                                </tr>
                                <tr>
                                    <th scope="row">{{crypto[2].type}}</th>
                                    <td> {{crypto[2].adresse}} </td>
                                    <td> <input type="button" class="btn btn-secondary" value="Copier" class="js-copy" data-target="#tocopy" style="margin-top:3px;  "> </td>
                                </tr>

                                </tbody>
                                <tfoot>
                                <tr>
                                    <th colspan="2">Montant a payer</th>
                                    <td>{{ montantTotal }} €</td>
                                </tr>
                                </tfoot>
                            </table>
                        </div>

                        <div class="mt-2" >
                           <h6>Continuer vers votre espace client  <a href="{{ path('app_compte')}}" class="text-warning"> cliquez ici </a></h6>
                        </div>
                    </div>

                </div>
            </div>
        </section>
        <!-- track-area-end -->
        {% include 'plusInfo.html.twig' %}

    </main>

{% endblock %}
